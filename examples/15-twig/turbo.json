{
    "extends": ["//"],
    "tasks": {
        "build": {
            "outputs": ["./dist/**"]
        },
        "clean": {
            "dependsOn": [
                "gulp-tron#build",
                "@gulp-tron/plugin-styles#build",
                "@gulp-tron/plugin-scripts#build",
                "@gulp-tron/plugin-utils#build"
            ]
        }
    }
}
