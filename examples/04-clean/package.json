{
  "name": "04-clean",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "gulp @build",
    "lint": "biome check",
    "clean": "gulp @clean"
  },
  "devDependencies": {
    "gulp-tron": "workspace:*",
    "gulp": "^5.0.1"
  }
}
