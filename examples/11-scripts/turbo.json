{
    "extends": ["//"],
    "tasks": {
        "build": {
            "outputs": ["www/{js,css}/**"]
        },
        "clean": {
            "dependsOn": ["gulp-tron#build", "@gulp-tron/plugin-scripts#build"]
        }
    }
}
