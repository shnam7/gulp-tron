{
    "name": "12-scripts",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "gulp @build",
        "build:ts": "tsc",
        "clean": "gulp @clean"
    },
    "dependencies": {
        "@gulp-tron/plugin-scripts": "workspace:*"
    },
    "devDependencies": {
        "gulp": "^4.0.2",
        "gulp-tron": "workspace:*",
        "rimraf": "^5.0.5",
        "typescript": "^5.3.3"
    }
}
