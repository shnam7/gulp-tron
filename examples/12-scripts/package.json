{
    "name": "12-scripts",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "gulp @build",
        "clean": "gulp @clean"
    },
    "devDependencies": {
        "@gulp-tron/plugin-utils": "workspace:*",
        "gulp": "^4.0.2",
        "gulp-tron": "workspace:*",
        "rimraf": "^5.0.5"
    }
}
