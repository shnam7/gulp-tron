'use strict'

console.log('This is sub-1')

class JSGreeter {
    // eslint-disable-line no-unused-vars
    constructor() {}
    greet(msg) {
        let square = x => x * x
        return `${msg}, num=${square(3)}`
    }
}
