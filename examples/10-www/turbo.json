{
    "extends": ["//"],
    "pipeline": {
        "build": {
            "outputs": ["www/{js,css,images}"]
        },
        "clean": {
            "dependsOn": ["gulp-tron#build"]
        }
    }
}
