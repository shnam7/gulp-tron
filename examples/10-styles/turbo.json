{
    "extends": ["//"],
    "tasks": {
        "build": {
            "inputs": ["assets/**"],
            "outputs": ["www/{css}/**"]
        },
        "clean": {
            "dependsOn": ["gulp-tron#build"]
        }
    }
}
